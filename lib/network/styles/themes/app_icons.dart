import 'package:flutter/material.dart';

class AssetPath {
  static const iconsearch = Icon(
    Icons.search,
    size: 30,
  );
  static const iconsetting = Icon(
    Icons.settings,
    size: 30,
  );
  static const iconnotifications = Icon(
    Icons.notifications_none_rounded,
    size: 30,
  );
  static const iconprofile = Icon(
    Icons.perm_identity_rounded,
  );
  static const iconradio = Icon(
    Icons.radio,
  );
  static const iconzingchart = Icon(
    Icons.area_chart,
  );
  static const icondelivery = Icon(
    Icons.change_circle_outlined,
  );
  static const icondmicro = Icon(
    Icons.mic_none_rounded,
    // color: Colors.white,
    size: 30,

  );
}
